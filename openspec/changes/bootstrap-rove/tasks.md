## 1. 固定依赖与验证集成入口

- [x] 1.1 根据官方发行和源码固定 Rust、Rig、EasyTier、Tauri、Vue 工具链版本，记录配套网络二进制来源、目标架构和必要发行说明。
- [x] 1.2 用独立验证程序确认选定 EasyTier 版本可由一个进程管理多个实例，并完成本机创建、停止、列出 peer 的调用；记录真实协议和配置字段。
- [x] 1.3 验证 Rig 的流式响应、兼容接口配置、自定义工具与取消接入，确认同一个模型响应中的多个工具可以顺序执行。
- [ ] 1.4 验证 Linux/macOS/Windows 的本机 socket 与后台服务接入方式，以及 Android/iOS 嵌入核心的构建边界，产出明确支持矩阵。
- [x] 1.5 将本提案两份 OpenAPI 的格式、引用、operationId 和示例校验接入开发检查，保持契约作为 wire schema 的来源。
- [x] 1.6 将详细需求 FR/NFR、用户故事 US 与能力规格和端点映射纳入验收记录，接口变更时检查相关文档同步。

## 2. Workspace、基础模型和本机持久化

- [x] 2.1 创建 design 中的 workspace、agent lib/bin、CLI 与 GUI 工程骨架，落实 Rust 命名和无循环依赖检查。
- [x] 2.2 在基础模块定义设备、网络、会话、作业和服务值类型，根据 OpenAPI 在协议模块生成或实现 DTO、错误、版本和 capabilities，并验证序列化契约。
- [x] 2.3 实现首次设备身份初始化、保留数据时复用身份和单数据目录实例锁；验证升级与重复启动行为。
- [x] 2.4 建立版本化 SQLite 初始化和必要表结构，确保客户端只能通过 agent 修改状态，并提供迁移前备份路径。
- [x] 2.5 实现设备模型配置与 `max_active_runs` 的读取/更新，校验正整数，默认日志和状态输出隐藏凭据字段。

## 3. 本机 socket API 与 SDK

- [ ] 3.1 实现 Unix domain socket 与 Windows named pipe 监听、长度前缀 JSON 帧、请求关联和同账号访问边界。
- [x] 3.2 实现 agent 的本机 API 路由、握手和统一错误映射，完成协议版本不兼容测试。
- [x] 3.3 实现 `rove-sdk` 本机客户端、事件订阅及连接恢复；确认 SDK 不依赖 agent crate。
- [x] 3.4 增加 CLI 最小入口，支持连接已有实例、查看设备状态与诊断启动失败；验证多个客户端同时访问同一状态。
- [x] 3.5 按 local-socket.md 验证拆帧/粘帧、correlation_id、operationId 二级参数校验、多订阅及 unsubscribe 不取消 run。

## 4. EasyTier 适配、多网络与远端访问

- [x] 4.1 实现 EasyTier 本机管理适配器，隔离上游类型并校验配套版本，保持管理端点仅本机可达。
- [x] 4.2 实现网络创建、保存、显式更新、启动、停止和删除，以及 Rove `network_id` 到本机 `instance_id` 的映射。
- [x] 4.3 实现可移植入网字段与本机专属字段分离，重复导入复用网络、冲突导入返回错误；验证设备身份不被覆盖。
- [x] 4.4 完成单设备单网络加入约束、EasyTier DHCP 实际地址读取、实例错误展示和物理路由冲突检测；验证切换与实例故障不删除其他配置。
- [x] 4.5 实现 overlay 上 HTTP/JSON 与 SSE 服务端，只开放规定的网络入口，处理地址变化和网络停止。
- [x] 4.6 实现 SDK 远端 transport 和本机 agent 转发，目标上下文贯穿请求与响应；禁止不可达时静默本机执行。
- [x] 4.7 实现 EasyTier 候选 peer 加 `hello` 的设备发现、稳定身份合并和地址刷新，区分普通节点、离线与不兼容设备。
- [x] 4.8 验证 overlay 成员可调用、物理 LAN/WAN 无法调用、本机管理端口不可远程访问，以及虚拟地址更换后的监听边界。

## 5. 网络分享与密文配置服务

- [x] 5.1 定义并实现版本化 `network_join_config` 和验证器，测试导出不包含设备、本机实例、模型、会话和服务秘密。
- [x] 5.2 实现客户端随机密钥、认证加密 envelope 与 URL fragment 解析，覆盖错误密钥、篡改、未知格式和失败后配置不变。
- [x] 5.3 实现独立 `rove-config-server` 的匿名密文上传/下载、随机 ID、七天到期及清理，并设置请求和存储资源上限。
- [x] 5.4 实现通过 agent 的分享、URL 导入和手动入网端点，支持选择托管服务地址；服务不可达时手动入口仍可使用。
- [x] 5.5 实现二维码生成与同 URL 导入流程的衔接，验证多设备重复使用和到期不影响已加入设备。

## 6. 会话、作业与并发调度

- [x] 6.1 实现目标设备创建、列出和读取会话历史；通过 SDK 从另一客户端继续同一会话。
- [x] 6.2 实现事务性作业接受，持久关联 `request_id`、请求摘要和 `run_id`；测试丢响应重试与同 ID 不同内容冲突。
- [x] 6.3 实现 session FIFO 和设备默认四个运行槽；只对可执行 session 队首占槽，避免同会话等待项阻塞其他会话。
- [x] 6.4 实现并发设置动态变更、可见排队及有限队列容量的明确错误；验证调低上限不取消已运行作业。
- [x] 6.5 实现有序 run 事件持久化、查询快照与流式订阅，覆盖序号水位、断线恢复、重复事件和输出截断。
- [x] 6.6 实现按 run 取消及启动时旧作业 `interrupted` 处理，测试排队取消、运行取消互不影响、重启不执行旧队列。
- [x] 6.7 使用可控假执行器验证同会话顺序、跨会话重叠、四个运行加第五个排队，以及失败后后续作业可推进。
- [x] 6.8 验证 queued 输入不提前进入模型上下文，Run 查询仍能展示待执行输入；校验历史过期、终态204和快照水位续接。

## 7. Rig 执行与通用命令工具

- [x] 7.1 实现 Rig 适配器：读取目标设备模型配置、运行时快照、加载上下文和转换流式输出；配置缺失返回明确错误。
- [x] 7.2 实现 `system_exec` 的工作目录、命令、退出码、输出和超时处理，操作使用 agent 当前 OS 身份。
- [x] 7.3 接入单 run 顺序工具调用与跨 run 命令并发，将工具结果归档到正确会话；不增加全设备命令锁。
- [x] 7.4 接入模型与受管理子进程取消，记录无法停止的结果；验证取消不误伤另一作业及已部署独立服务。
- [x] 7.5 实现 unsupported 平台结果和必要资源上限，补充权限错误、包管理器锁和端口竞争的工具结果测试。
- [x] 7.6 使用假提供方完成持久化/失败测试，并在有可用模型配置时记录一次真实流式工具执行验收。

## 8. 服务登记与 TCP 代理

- [x] 8.1 实现服务模型、回环目标校验、端口分配和指定 overlay 入口的 TCP 双向转发。
- [x] 8.2 实现代理建立与持久登记的一致性处理，监听或数据库失败时释放资源并报告真实状态。
- [x] 8.3 实现服务查询、HTTP/HTTPS URL 和 `access_info` 展示，通过 SDK 从远端取得目录。
- [x] 8.4 实现取消发布、网络停止、地址刷新和 agent 重启恢复，原端口冲突时明确报告，保持目标应用与数据不变。
- [x] 8.5 将 `publish_service` 接入 Rig 工具与普通 API，验证 AI 和 CLI 使用同一服务逻辑。
- [ ] 8.6 完成两设备服务访问测试，覆盖应用认证、网络不可达、恢复和取消发布后目标仍运行。
- [x] 8.7 按 OpenAPI 实现服务配置 PUT，验证保留身份与网络、端口变更失败保留旧发布、取消发布幂等。

## 9. CLI 完整交互闭环

- [x] 9.1 完成网络与设备子命令，覆盖创建、分享、URL/手动加入、启停删除和设备选择。
- [x] 9.2 完成模型和并发设置、会话列表/继续、作业提交/事件/查询/取消，以及明确目标展示。
- [x] 9.3 完成服务发布、列表、认证信息查看、地址输出、取消发布和 agent 运行状态子命令。
- [x] 9.4 完成 `rove` 交互入口、帮助、`--json`、stderr 诊断及退出码；验证无 TTY 时不无限等待输入。

## 10. Vue GUI 与移动壳

- [x] 10.1 实现 Tauri 到 SDK 的薄桥接和事件订阅生命周期，Vue 页面不直接修改数据库或调用 EasyTier 管理接口。
- [ ] 10.2 完成网络列表、创建与导入、二维码展示和手动配置界面，扫码能力调用同一导入入口。
- [x] 10.3 完成设备选择、模型设置、并发设置和明确的离线/不兼容/unsupported 提示。
- [x] 10.4 完成多会话界面、排队和执行状态、输出恢复与单作业取消；验证多个窗口不阻塞其他会话。
- [ ] 10.5 完成服务卡片、地址/认证信息查看、打开服务和取消发布，并验证与 CLI 的逐项能力对等。
- [ ] 10.6 在 Android/iOS 壳嵌入同一 agent library，验证本机协议与不支持操作反馈；标注 native 网络和后台能力未交付。

## 11. 打包与系统生命周期

- [ ] 11.1 组装 Linux 目标的 Tauri 桌面包和无界面包，注册网络系统服务与用户/指定账号 agent，验证关闭 GUI 后持续运行。
- [ ] 11.2 组装 Windows 目标程序和安装包，验证 CLI 路径、named pipe、后台实例及系统网络服务安装。
- [ ] 11.3 组装 macOS 目标程序和安装包，验证 CLI、用户 agent 与网络服务安装及平台授权失败提示。
- [ ] 11.4 为每项声明支持的系统/架构记录安装、启动、升级和保留数据卸载结果，不将未验证平台标记为完整支持。
- [ ] 11.5 产出移动壳构建与能力验证记录，以及后续 native VPN/后台接入需要的平台资源说明。
- [x] 11.6 编写配置服务部署说明和开发本地启动方法；常规组网运行不依赖配置服务在线。

## 12. 端到端验收与交付记录

- [ ] 12.1 在真实两设备完成创建网络、分享加入、稳定身份发现、配置远端模型、AI 装配服务并从另一设备访问的闭环。
- [x] 12.2 验证客户端切换和断线重连、并发取消隔离、agent 重启中断，以及服务重启恢复；记录实际结果。
- [x] 12.3 验证个人多网络保存与分享、单设备单网络切换、并发启动互斥、地址变化、物理接口访问拒绝和分享过期，确保不误称成员已撤销或目标已回滚。
- [ ] 12.4 完成 GUI/CLI 功能对等矩阵与七份规格的场景映射，运行必要 Rust、前端和平台检查，记录未交付的移动扩展范围。
- [x] 12.5 使用 OpenAPI 与共享示例验证33个 agent 操作在 SDK/socket/HTTP上的对应关系，以及三个配置服务操作；验证HTTP状态、分页、错误、SSE与实际序列化一致。
