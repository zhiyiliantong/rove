# 阶段归档记录

归档日期：2026-09-21。用户明确要求归档本提案，后续采用小提案迭代。

本提案完成 84/93 项，剩余 9 项保留未勾选：1.4、10.2、10.6、11.2、11.3、11.4、11.5、12.4、14.2。规划产出物完整不代表上述平台、官方账号授权及验收全部完成。

已交付核心：设备身份、对等 agent/SDK/CLI/GUI 基础、Linux 多网络、网络分享、模型目录与 API 同步、持久会话与远端执行关联、服务发布、双语正式 GUI 和 Android 验收包。具体环境、限制及测试以仓库 `docs/implementation-status.md` 和各交付记录为准。

七份能力规格同步至 `openspec/specs/`；它们是行为契约，平台实际支持必须结合支持矩阵。原型的现有主规格保留。现行 OpenAPI、用例和 socket 映射复制至根目录 `api/`，构建与校验切换到固定目录；本目录 `api/` 是归档快照。

后续计划见仓库 `docs/next-iterations.md`，逐项映射原未完成任务；优先 Windows 组网/安装、Android 网络/后台和双机验收，账号登录按提供方拆开，Apple 平台继续暂缓。本次只整理待建立提案清单，没有自动创建或实现这些新提案。

归档核验：七份主规格的 55 条需求与增量逐项一致，五份现行接口/用例/传输文档与快照内容一致。归档后 OpenSpec 全量 strict 11/11 通过，协议模块测试 7/7、原型契约回归 14/14 通过；契约检查覆盖 52 个操作、409 个引用、21 个示例、35 个正反用例，以及 FR/NFR/用户故事映射。Rust 格式和 diff 检查通过。原活动路径不再被源码或检查脚本引用；验收脚本显式读取本归档的冻结 FR/US 基线，并核对现行主规格存在。`.openspec.yaml` 随目录保留。
